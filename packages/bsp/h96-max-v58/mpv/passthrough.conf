# /etc/mpv/passthrough.conf -- managed by h96-audio-passthrough.
#
# This is the shipped DEFAULT (mode: off). It is NOT included by mpv.conf until
# you run `h96-audio-passthrough bitstream` or `... lpcm`, which rewrite this
# file for the chosen mode/device and add an `include=` block to mpv.conf.
#
# Leaving this file here (unused) is harmless. To see what an active fragment
# looks like, run the tool and re-read this file.
#
# No options are set here in the off state.
